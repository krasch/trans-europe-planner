{
  "comment": "only using this for running tests, not necessary for building the project",
  "name": "trans-europe-planner",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "test": "node node_modules/.bin/jest",
    "check-pretty": "node node_modules/.bin/prettier script/ style/*.js --check",
    "make-pretty": "node node_modules/.bin/prettier script/ style/*.js --write"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "prettier": "3.3.3",
    "jest-environment-jsdom": "^29.7.0"
  }
}
